/** @type {import('next').NextConfig} */
/** @type {import('next-i18next').UserConfig} */

const nextConfig = {
    i18n: {
		defaultLocale: 'en',
		locales: ['en', 'id']
	}
};

export default nextConfig;
